# Backend Dockerfile
FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
# Only install production dependencies for backend if possible, 
# but here we use same package.json so we install all.
RUN npm install

COPY . .

EXPOSE 3000

CMD ["node", "server/index.js"]
